# IntelimonDashboard
An interactive dashboard that will summarize the results of IntELimon outputs across multiple Terrestrial  LiDAR Scans (TLS). The dashboard will both become the web interface for the IntELimon data as well as being a stand alone program that can be run on a local machine. 
